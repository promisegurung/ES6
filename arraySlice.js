const fruits = ['apple', 'banana', 'carrot', 'dew', 'eggplant'] // 0 1 2 3 4 

const slice = fruits.slice(1,3); // index 1 and index 2: IGNORE index 3 //
slice;

const secondSlice = fruits.slice(3, );
secondSlice;